<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="RdpReaper.Gui.Views.PolicyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="12">
            <TextBlock FontSize="24" FontWeight="SemiBold" Text="Policy" />
            <StackPanel Orientation="Horizontal" Spacing="12">
                <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" Width="18" Height="18" />
                <Button Content="Refresh" Click="OnRefreshClicked" />
                <Button Content="Save" Click="OnSaveClicked" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="12" Margin="0,16,0,0">
            <TextBlock
                Foreground="DarkRed"
                Text="{x:Bind ViewModel.ErrorText, Mode=OneWay}"
                TextWrapping="Wrap"
                Visibility="{x:Bind ViewModel.ErrorVisibility, Mode=OneWay}" />

            <StackPanel Spacing="6">
                <TextBlock Text="IP Failure Threshold" />
                <TextBox Text="{x:Bind ViewModel.IpFailureThreshold, Mode=TwoWay}" Width="200" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="IP Window Seconds" />
                <TextBox Text="{x:Bind ViewModel.IpWindowSeconds, Mode=TwoWay}" Width="200" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="IP Ban Duration Seconds" />
                <TextBox Text="{x:Bind ViewModel.IpBanDurationSeconds, Mode=TwoWay}" Width="200" />
            </StackPanel>

            <ToggleSwitch
                Header="Firewall Enabled"
                IsOn="{x:Bind ViewModel.FirewallEnabled, Mode=TwoWay}" />

            <TextBlock FontSize="16" FontWeight="SemiBold" Text="Allow / Block Lists" Margin="0,12,0,0" />

            <StackPanel Spacing="6">
                <TextBlock Text="Allow IPs (one per line)" />
                <TextBox
                    Text="{x:Bind ViewModel.AllowIpList, Mode=TwoWay}"
                    AcceptsReturn="True"
                    Height="100" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="Block IPs (one per line)" />
                <TextBox
                    Text="{x:Bind ViewModel.BlockIpList, Mode=TwoWay}"
                    AcceptsReturn="True"
                    Height="100" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="Allow Subnets (one per line, e.g. 203.0.113.0/24)" />
                <TextBox
                    Text="{x:Bind ViewModel.AllowSubnetList, Mode=TwoWay}"
                    AcceptsReturn="True"
                    Height="100" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="Block Subnets (one per line, e.g. 203.0.113.0/24)" />
                <TextBox
                    Text="{x:Bind ViewModel.BlockSubnetList, Mode=TwoWay}"
                    AcceptsReturn="True"
                    Height="100" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
